{% extends 'base.html.twig' %}

{% block title %}Support{% endblock %}

{% block body %}
    <h1>Support</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ support.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ support.url }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ support.type }}</td>
            </tr>
            <tr>
                <th>Id_evenementAssocie</th>
                <td>{{ support.idEvenementAssocie }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ support.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_support_index') }}">back to list</a>

    <a href="{{ path('app_support_edit', {'id': support.id}) }}">edit</a>

    {{ include('support/_delete_form.html.twig') }}
{% endblock %}
